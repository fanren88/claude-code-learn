---
interface Props {
	id?: string;
	title: string;
	lead?: string;
	eyebrow?: string;
}

const { id, title, lead, eyebrow } = Astro.props;
---

<section class:list={['hub-section', id && `hub-section--${id}`]} id={id}>
	<header class="hub-section-head">
		{eyebrow && <p class="hub-section-eyebrow">{eyebrow}</p>}
		<h2>{title}</h2>
		{lead && <p>{lead}</p>}
	</header>
	<slot />
</section>
